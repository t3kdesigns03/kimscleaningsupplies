# Deploy the Next.js storefront from this GitHub repo.
# GitHub Pages still serves the static HTML at the repo root.

[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"