# Copy to .env.local for local dev. Set the same names in Netlify
# (Site configuration > Environment variables). Never commit real values.
NEXT_PUBLIC_SUPABASE_URL=https://lncmqmlckdrngrpdcvgu.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# server only — used by /admin and /api/admin/*; never exposed to the browser
SUPABASE_SERVICE_ROLE_KEY=
# shared staff password for /admin
ADMIN_PASSWORD=
# empty = PayPal buttons hidden; pickup/show orders still work (pay in person)
NEXT_PUBLIC_PAYPAL_CLIENT_ID=
