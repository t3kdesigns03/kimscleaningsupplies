# Claude prompt — admin header = store header + Home

Live:
- Store header spec: https://kimscleaningproducts.com/
- Admin: https://kimscleaningproducts.com/admin

The order cards are fine. The admin chrome is not the store. Attach the homepage screenshot (promo bar + cream nav + globe + wordmark). That is the header. Copy it onto `/admin` and the staff login screen.

## Header (login + logged-in admin)

Same stack as the store, top to bottom:

1. Dark green promo bar — reuse the live next-event line (“Next stop: Apple & Pork Fest — Clinton, IL”) or a short “Staff · orders”.
2. Cream nav row:
   - Left: globe mark + “Kim’s Cleaning Products” + subline ECO EASY MICROFIBER (same type as `/`)
   - Right: **Home** (link `/`) · Refresh · Print list · Sign out
   - Login screen right side: **Home** only
3. No extra green admin strip. No tiny orphan logo.

Logged-in page title “Orders” + count stays **under** that header, on the mint field, not instead of the wordmark.

## Login

Keep the card. Same header above it. Add a visible **Home** control (header link is enough). “Open orders” button uses the store lime, not gray.

## Warmth

Same tokens as `/`: cream `#faf7f0` / mint field / earth green / lime CTA. Do not invent a new palette.

## Do not

- Change filters, cards, status buttons, Supabase
- Put a hero globe banner on `/admin` (header only)
- Add `/admin` to the public store nav

Phone: Home + Sign out stay tappable. Margins off. Push when admin header is indistinguishable from the store header except for Home / Refresh / Print / Sign out.
